spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rnzmndz/project-site-monitoring-config.git
          default-label: ${SPRING_PROFILES_ACTIVE}
          clone-on-start: true